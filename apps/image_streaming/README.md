# ImageStreaming

Server side component for receiving images from the camera devices, and streaming down to the web browsing clients.

Well maybe stretching the term streaming - it receives JPEGs from the camera, over a websocket, then send the same JPEG down to the interested clients.